# +required The configuration file schema version
schemaVersion: 1.0

# +optional Outgoing connection details for the component.
dependencies:
    serviceReferences:
    - name: choreo:///teeorg/api-key/org-api-key/803f0/v1/ORGANIZATION
      connectionConfig: 01ef984e-746d-1d20-a5b9-6a69aa2d277a
      env:
        # +required Key name of the connection configuration.
        - from: ServiceURL
          # +required Environment variable injected into the container.
          to: BAL_CONFIG_VAR_SERVICEURL
        - from: ChoreoAPIKey
          to: BAL_CONFIG_VAR_CHOREOAPIKEY
